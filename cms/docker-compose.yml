services:
  postgres:
    restart: always
    image: postgres:18
    environment:
      POSTGRES_PASSWORD: password
      POSTGRES_DB: monk-cms
    volumes:
      - monk-cms-pgdata:/var/lib/postgresql
    ports:
      - "5432:5432"
      
volumes:
  monk-cms-pgdata:
