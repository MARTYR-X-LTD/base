---
import type { SerializedQuoteNode } from '@payloadcms/richtext-lexical'
import type { LexicalNode } from '@types/lexical'
import LexicalRenderer from '@components/Lexical/LexicalRenderer.astro'

interface Props {
  node: SerializedQuoteNode
}

const { node } = Astro.props
---

<blockquote><LexicalRenderer nodes={node.children as LexicalNode[]} /></blockquote>
